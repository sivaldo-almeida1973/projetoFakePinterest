import secrets
# Importa o módulo secrets, que fornece métodos para gerar números seguros para fins de criptografia

print(secrets.token_hex(16))
# Gera um token hexadecimal seguro de 16 bytes e o imprime
